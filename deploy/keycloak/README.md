# Keycloak local (laptop)

Copies of the OptimizeSolux common-infra realm + login theme for autonomous `docker compose up`.

- Source of truth: `optimize-common-infra/images/keycloak/`
- After editing common-infra, re-copy realm JSON and `themes/biocollect/` here (or sync manually).

Local Keycloak: http://localhost:8180 (admin / admin)
Realm: `biocollect` · client: `biocollect-web` · theme: `biocollect`
